node_modules/
dist/
.env
.env.json
.env.local

# Python
__pycache__/
*.pyc

# Generated linter output at the reports/ root (the dashboard only reads
# reports/{site}/audits/). Commit deliverables explicitly with `git add -f`.
/reports/*.md
/reports/*.json
/reports/*.txt

# Personal/local editor + AI tool settings (per-machine, not shared)
**/settings.local.json

# Local "where did we leave off" log for Claude Code / Copilot (see
# CLAUDE.md and .github/copilot-instructions.md). Machine-local, not shared.
PROJECT_LOG.md
